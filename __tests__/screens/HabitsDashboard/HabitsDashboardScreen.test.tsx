import { HabitCard } from '../../../src/screens/HabitsDashboard/components/HabitCard'

describe('HabitCard', () => {
    it('exposes priority and accessible reorder actions', () => {
        expect(HabitCard).toBeDefined()
    })
})
